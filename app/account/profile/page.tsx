import { AccountProfilePage } from "../AccountPages";

export const metadata = { title: "Profile details", robots: { index: false, follow: false } };

export default function Profile() {
  return <AccountProfilePage />;
}
