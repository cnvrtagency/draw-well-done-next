import { AccountResponsiblePlayPage } from "../AccountPages";

export const metadata = { title: "Responsible play", robots: { index: false, follow: false } };

export default function ResponsiblePlay() {
  return <AccountResponsiblePlayPage />;
}
