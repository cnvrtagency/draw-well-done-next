import { AccountWinsPage } from "../AccountPages";

export const metadata = { title: "My wins", robots: { index: false, follow: false } };

export default function Wins() {
  return <AccountWinsPage />;
}
