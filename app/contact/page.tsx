import type { Metadata } from "next";
import { Contact } from "@/components/StaticPages";

export const metadata: Metadata = {
  title: "Contact TopDraw | Prize Competition Support",
  description: "Contact TopDraw for support with competitions, tickets, prize claims, accounts, payments, free postal entry or general questions.",
  alternates: { canonical: "/contact" },
};

export default Contact;
