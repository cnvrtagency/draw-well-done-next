import type { Metadata } from "next";
import { ResponsiblePlay } from "@/components/StaticPages";

export const metadata: Metadata = {
  title: "Responsible Play | TopDraw UK Competitions",
  description: "Read TopDraw’s responsible play guidance, including 18+ UK-only participation, sensible spending, account safety and support resources.",
  alternates: { canonical: "/responsible-play" },
};

export default ResponsiblePlay;
