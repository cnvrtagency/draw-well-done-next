import type { Metadata } from "next";
import { FreeEntry } from "@/components/StaticPages";

export const metadata: Metadata = {
  title: "Free Postal Entry Route for TopDraw Competitions",
  description: "Learn how to enter eligible TopDraw competitions using the free postal entry route, including what to send, where to send it and how entries are processed.",
  alternates: { canonical: "/free-entry" },
};

export default FreeEntry;
